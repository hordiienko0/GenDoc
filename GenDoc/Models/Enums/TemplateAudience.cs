namespace GenDoc.Models.Enums
{
    /// <summary>Для кого призначений шаблон. Розділяє видимість, а не права:
    /// шаблон постійного складу не з'являється у звичайній генерації й пакетах,
    /// а звичайний - у документах постійного складу. Раніше такого поняття не
    /// було взагалі, і в постійному складі показувався весь перелік підряд.</summary>
    public enum TemplateAudience
    {
        /// <summary>Набори - стара, звична поведінка. Замовчування навмисне:
        /// усі шаблони, заведені до появи поділу, лишаються там, де були.</summary>
        Intake = 0,

        /// <summary>Постійний склад.</summary>
        PermanentStaff = 1
    }
}
