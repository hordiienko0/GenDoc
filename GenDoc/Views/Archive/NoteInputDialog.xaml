<Window x:Class="GenDoc.Views.Archive.NoteInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Style="{StaticResource {x:Type Window}}"
        Title="{Binding Title}" Width="420" SizeToContent="Height"
        Background="{StaticResource PanelBrush}"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <StackPanel Margin="20">
        <TextBlock Text="{Binding FileName}" FontSize="12.5" FontWeight="SemiBold"
                   Foreground="{StaticResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis"/>

        <TextBlock Text="Примітка" FontSize="11.5" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,12,0,4"/>
        <TextBox Text="{Binding Note, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True"
                 MinHeight="56" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Скасувати" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding CancelCommand}"/>
            <Button Content="OK" Margin="8,0,0,0" MinWidth="90" Command="{Binding OkCommand}"/>
        </StackPanel>
    </StackPanel>
</Window>
